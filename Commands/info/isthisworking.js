const Discord = require("discord.js")

module.exports = {
    name: "work",
    aliases: ["working.itv"],
    category: "info",
    run: async (client, message, args) => {
    message.channel.send("iam working right now") }
  };


        